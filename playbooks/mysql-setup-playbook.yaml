- name: Create directory on database nodes
  hosts: mmai-database
  become: yes
  tasks:
    - name: Ensure directory exists
      ansible.builtin.file:
        path: /mnt/disks/mmai-mysql-billing
        state: directory
        mode: '755'
